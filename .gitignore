# Ambiente virtual
.venv/
venv/
env/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/
.eggs/
dist/
build/

# IDEs / OS
.idea/
# Versionamos `.vscode/tasks.json` (build = instalar + Streamlit); resto local.
.vscode/*
!.vscode/tasks.json
*.swp
.DS_Store
Thumbs.db

# Segredos locais
.env
.env.*

# Contexto institucional local (Gamma / narrativa) — não versionar por defeito
.passos_gamma_context.txt

# Ambiente Python acidental dentro de notebooks (não versionar; usar .venv na raiz).
notebooks/venv/

# Artefatos gerados pelo projeto (ETL / treino — recriar com run.bat)
data/dados.parquet
data/resumo_anual.pdf
models/*.joblib

# Streamlit
.streamlit/secrets.toml

# Atalho local (contém caminhos absolutos; recriar com criar_atalho.ps1)
Passos Mágicos.lnk
