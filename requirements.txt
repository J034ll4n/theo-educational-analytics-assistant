# Versões fixas para deploy reprodutível (MVP / Streamlit Cloud).
# Geradas a partir de um ambiente onde `pip install -r requirements.txt` antigo já resolvia bem as dependências.
# Se `pip install` falhar numa máquina nova, use `pip install --upgrade pip` e tente de novo; em último caso, alivie uma linha.

streamlit==1.56.0
duckdb==1.5.1
pandas==3.0.2
pyarrow==23.0.1
plotly==6.7.0
scikit-learn==1.8.0
joblib==1.5.3
langchain==1.2.15
langchain-community==0.4.1
langchain-core==1.2.28
kaleido==1.2.0
shap==0.51.0
numpy==2.4.4
imbalanced-learn==0.14.1
xgboost==3.2.0
requests>=2.31.0,<3
